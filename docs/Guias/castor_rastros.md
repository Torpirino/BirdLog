# Hoja-guía — Rastros de castor *(borrador)*

Tabla destino: `visitas` + `castor_rastros`.
Reglas de formato y validación: `docs/Guias/formato-guias.md`.

> **Borrador.** Los vocabularios cerrados de `tipo_rastro`,
> `intensidad_rastro`, `reciente_antiguo` y el significado de
> `aplicacion` están pendientes de confirmar con el cliente
> (`docs/Revisar con GABI.md` §5). Hasta entonces se tratan como texto
> libre. Las fotos van en la tabla `fotos` (`tabla_origen =
> 'castor_rastros'`), no en esta hoja.

`lugar` ∈ catálogo `lugares`. Obligatorio del rastro: `tipo_rastro`.
`especie` en singular del catálogo (normalmente `Castor europeo`).

## Visita

| tipo_visita | fecha | hora_inicio | hora_fin | lugar | observador | observaciones_visita |
|---|---|---|---|---|---|---|
| CASTOR_RASTROS |  |  |  |  |  |  |

## Rastros (una fila por rastro)

| especie | tipo_rastro | intensidad_rastro | reciente_antiguo | aplicacion | observaciones |
|---|---|---|---|---|---|
|  |  |  |  |  |  |
|  |  |  |  |  |  |
